<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ee461l_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".UploadSearchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".SearchResultsActivity" />
        <activity android:name=".ViewInformationActivity" />
        <activity android:name=".ProfileViewActivity" />
        <activity android:name=".InitializationActivity" />
        <activity android:name=".UpdatePersonalProfileActivity" />
        <activity android:name=".UpdateBusinessProfileActivity" />
        <activity android:name=".ViewYourContactsActivity"></activity>
    </application>

</manifest>